admm.algorithm <- function(SigmaX, SigmaY, SigmaXY D){
  conv = FALSE
  #### Thsi algorithm is not going to be great --- it requires being able to handle storing 
  ####  Sigma_X, Sigma_y, Sigma_XY
  while(conv == FALSE){
    # Update F
    # Update G
    # Update H
    
  }
  return(F)
}